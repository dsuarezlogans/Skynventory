<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ve.com.dsuarez.skynventory.LoginActivity"
    android:background="@drawable/gradient">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/etEmail"
        android:hint="@string/emailHint"
        android:layout_marginTop="20dp"
        android:textColorHighlight="@android:color/white"
        android:textColor="@color/icons"
        android:textColorHint="@color/hintLight"
        android:textColorLink="@android:color/white"
        android:drawableLeft="@drawable/ic_mail_outline_black_24dp"
        android:layout_above="@+id/etPassword"
        android:layout_alignParentStart="true"
        android:fontFamily="sans-serif-light"
        android:paddingLeft="10dp"
        android:drawableTint="@color/hintLight"
        android:drawablePadding="10dp"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/etPassword"
        android:hint="@string/passHint"
        android:layout_marginTop="20dp"
        android:textColor="@color/icons"
        android:textColorHint="@color/hintLight"
        android:textColorHighlight="@android:color/white"
        android:textColorLink="@android:color/white"
        android:drawableLeft="@drawable/ic_vpn_key_black_24dp"
        android:layout_above="@+id/bLogin"
        android:layout_alignParentEnd="true"
        android:fontFamily="sans-serif-light"
        android:paddingLeft="10dp"
        android:drawableTint="@color/hintLight"
        android:drawablePadding="10dp"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ingresar"
        android:id="@+id/bLogin"
        android:background="@drawable/butonstate"
        android:layout_marginBottom="95dp"
        android:layout_marginTop="20dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textColor="@color/icons"
        android:elevation="4dp"
        android:drawableLeft="@drawable/ic_input_black_18dp"
        android:drawableTint="@color/icons"
        android:drawablePadding="5dp"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/ivLogo"
        android:src="@mipmap/logo"
        android:minHeight="150dp"
        android:minWidth="150dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="65dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:text="Olvido su Contraseña?"
        android:id="@+id/tvOlvidoCon"
        android:textColor="#7FFFFFFF"
        android:layout_marginBottom="40dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
